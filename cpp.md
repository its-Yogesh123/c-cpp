

** OOPs **


1. Can any parameterized constructor be a default constructor?
    Yes 
2. Can we define any private method outside the class ?
    Yes
3. Can we 'declare' any private static member variable outside the class ?
    No - Declaration must be within class
4. Can we 'define' any private static member variable outside the class ? [code](./oops.cpp#L12)
    Yes - but either in namespace or global scope
5. Without declaratoin can we define any method/constructor outside the class using '::' ?
    No - Declaration is must for everything no exception
6. Uniform Initialization
7. Can we allocate object in stack using 'new' ?