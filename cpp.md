

# OOPs


1. Can any parameterized constructor be a default constructor?
    Yes 
2. Can we define any private method outside the class ?
    Yes
3. Can we 'declare' any private static member variable outside the class ?
    No - Declaration must be within class
4. Can we 'define' any private static member variable outside the class ? [code](./oops.cpp#L12)
    Yes - but either in namespace or global scope
5. Without declaratoin can we define any method/constructor outside the class using '::' ?
    No - Declaration is must for everything no exception
6. Uniform Initialization
7. Can we allocate object in stack using 'new' ?
8. 
    int* a;
    *a = 90; 

    // is this valid ?
9. What of alignas() why we need this?
10. Base obj;
    Base obj2;
    obj = obj2
    which  type of constructor/method will called here
11. Which(constructor/desrutctor) can be overloaded or inherited ?
12. How do you prevent an object intializaton in stack memory ?
13. Can we initialize an object in stack even by using private destructor ?
14. Can we make a derived class a friend class ?
15. How do you initilize  a const non-static member ?
15. Using delete this in destructor ?
15. Using delete this in constructor ?
16. What is this Class obj(); ?
16. Can we use 'delete' with every type of object ?